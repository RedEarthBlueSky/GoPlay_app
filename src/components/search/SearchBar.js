import React from "react";
import { Text, StyleSheet, View } from "react-native";
import styles from '../../../GlobalStyles'

const SearchBar = () => {
  return (
         <View>
           <Text style={styles.h1}>Search Bar</Text>
         </View>
       )
};

export { SearchBar }
